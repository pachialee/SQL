SELECT *
FROM superstore
ORDER BY price;

SELECT SUM(price)
FROM superstore;

SELECT MAX(price)
FROM superstore;

SELECT Min(price)
FROM superstore;

SELECT price
FROM superstore
ORDER BY 'Kitchen Supplies';

--How many Cutlery Sets are in stock?
SELECT SUM('Cutlery Sets'), stock_quantity
FROM superstore;
